general:
  stop_time: 5
network:
  graph:
    type: 1_gbit_switch
hosts:
  testnode:
    network_node_id: 0
    processes:
    - path: ../../target/debug/test_ipv6
      # no --shadow-passing filter: the dual-stack test is an intentional
      # "not yet implemented" marker; --summarize reports all results
      args: --summarize
      start_time: 1
